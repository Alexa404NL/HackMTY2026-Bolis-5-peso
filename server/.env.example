API_ROUTER=
DATABASE_URL=postgres://usuario:password@host:puerto/tsdb?sslmode=require
# Opcionales
# MODELO=google/gemini-2.5-flash
# Orígenes permitidos por CORS (por defecto localhost). Ej. producción:
# ALLOWED_ORIGIN_REGEX=https://([a-z0-9-]+\.)*vercel\.app|http://localhost(:\d+)?
